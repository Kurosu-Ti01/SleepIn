<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/docs/SleepIn-Docs/docs/dev-feature/1.update-check.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/docs/SleepIn-Docs/docs/dev-feature/1.update-check.md" />
              <option name="updatedContent" value="---&#10;title: 应用更新检测与下载实现（GitHub + WorkManager + DownloadManager）&#10;createTime: 2026/03/26 23:30:00&#10;---&#10;&#10;# 应用更新检测与下载实现&#10;&#10;本文完整说明 SleepIn 当前“检查更新 + 弹窗展示 Release Notes + 下载 APK”的实现方案，覆盖业务目标、架构拆分、关键代码入口、状态流转和验证方法。&#10;&#10;## 1. 功能目标与约束&#10;&#10;### 1.1 业务目标&#10;&#10;- 自动检测 GitHub 仓库是否发布了新版本。&#10;- 检测到新版本后，向用户展示更新弹窗。&#10;- 弹窗中展示发布说明（Release Notes）。&#10;- 用户点击“下载更新”后，直接使用系统 `DownloadManager` 下载 APK，不跳转浏览器。&#10;&#10;### 1.2 技术约束&#10;&#10;- 继续使用现有 **manual DI**，不引入 Hilt。&#10;- 不影响课表主流程，更新逻辑必须可独立失败。&#10;- 更新状态通过 `DataStore` 持久化，确保 UI 与后台任务共享同一份状态。&#10;- 后台任务通过 `WorkManager`，满足进程被杀后仍可执行的能力。&#10;&#10;## 2. 架构落位&#10;&#10;### 2.1 Domain 层&#10;&#10;- `domain/model/AppUpdateCheckResult.kt`&#10;  - `AppReleaseInfo`：标准化 release 元数据（版本、标题、notes、下载地址）。&#10;  - `AppUpdateCheckResult`：检查结果（有更新、已最新、失败、跳过）。&#10;- `domain/repository/UpdateRepository.kt`&#10;  - 抽象更新检查接口：`checkForUpdate(currentVersionName: String)`。&#10;- `domain/usecase/settings/PerformUpdateCheckUseCase.kt`&#10;  - 调用 `UpdateRepository` 检查更新。&#10;  - 将结果写回 `AppSettings`（是否有更新、release notes、最近错误、检查时间等）。&#10;&#10;### 2.2 Data 层&#10;&#10;- `data/update/GitHubUpdateRepositoryImpl.kt`&#10;  - 调 GitHub API：`/repos/Kurosu-Ti01/SleepIn/releases/latest`。&#10;  - 解析 `tag_name`、`name`、`body`、`assets`、`html_url`。&#10;  - 优先选择 universal APK，否则选择第一个 APK 资产。&#10;- `data/update/VersionNameComparator.kt`&#10;  - 比较本地版本与远端版本（兼容 `v1.2.3` 形式）。&#10;- `data/repository/SettingsRepositoryImpl.kt`&#10;  - 将更新状态读写到 DataStore。&#10;  - 支持设置备份导入导出中的更新字段。&#10;&#10;### 2.3 App / UI / Worker 层&#10;&#10;- `update/UpdateCheckScheduler.kt`&#10;  - 管理周期检查与立即检查任务。&#10;- `update/UpdateCheckWorker.kt`&#10;  - 后台执行检查更新 use case。&#10;- `MainActivity.kt`&#10;  - 根层监听 `settings`。&#10;  - 满足条件时弹出“发现新版本”弹窗，展示 `latestReleaseNotes`。&#10;- `update/ApkDownloadManager.kt`&#10;  - 封装 `DownloadManager` 下载入队逻辑。&#10;- `ui/screen/settings/SettingsScreen.kt`&#10;  - 手动“立即检查”和“下载更新”入口。&#10;&#10;## 3. 关键状态模型&#10;&#10;更新能力依赖 `AppSettings` 中的以下字段：&#10;&#10;- `autoCheckUpdateEnabled`: 是否允许后台自动检查。&#10;- `updateAvailable`: 当前是否有可用更新。&#10;- `latestRemoteVersion`: 远端最新版本标签。&#10;- `latestReleaseNotes`: 远端 release notes 文本。&#10;- `latestApkDownloadUrl`: APK 下载链接。&#10;- `dismissedUpdateVersion`: 用户“稍后”或已确认下载的版本，用于避免重复弹窗。&#10;- `lastUpdateCheckError`: 最近一次检查错误信息。&#10;- `lastUpdateCheckAtMillis`: 最近一次检查时间戳。&#10;&#10;## 4. 端到端数据流&#10;&#10;### 4.1 自动检查链路&#10;&#10;1. `SleepInApplication` 启动后观察 `autoCheckUpdateEnabled`。&#10;2. `UpdateCheckScheduler.syncPeriodic(...)` 同步 WorkManager 周期任务。&#10;3. `UpdateCheckWorker` 执行 `PerformUpdateCheckUseCase`。&#10;4. use case 将结果写入 `AppSettings`。&#10;5. `MainActivity` 通过 `observeSettingsUseCase()` 感知更新状态变化。&#10;&#10;### 4.2 手动检查链路&#10;&#10;1. 设置页点击“立即检查”。&#10;2. `SettingsViewModel.checkForUpdates()` 以 `force = true` 调用 use case。&#10;3. 检查结果更新 DataStore，页面与根层弹窗自动刷新。&#10;&#10;### 4.3 弹窗与下载链路&#10;&#10;1. `MainActivity` 判断：&#10;   - `updateAvailable == true`&#10;   - `latestApkDownloadUrl` 非空&#10;   - `latestRemoteVersion` 非空&#10;   - `latestRemoteVersion != dismissedUpdateVersion`&#10;2. 显示更新弹窗并渲染 `latestReleaseNotes`。&#10;3. 用户点击“下载更新”后：&#10;   - 调用 `ApkDownloadManager.enqueueApkDownload(...)`。&#10;   - 写入 `dismissedUpdateVersion = latestRemoteVersion`，避免重复弹窗。&#10;4. 下载进度与完成通知由系统 `DownloadManager` 管理。&#10;&#10;## 5. GitHub 解析策略&#10;&#10;当前解析策略如下：&#10;&#10;- 仅使用 `latest release` 接口。&#10;- 如果 release 是 `prerelease`，默认不作为升级目标。&#10;- `assets` 选择优先级：&#10;  1. 文件名包含 `universal` 且后缀 `.apk`&#10;  2. 第一个后缀 `.apk` 的资产&#10;- `body` 直接作为 release notes 文本显示。&#10;&#10;## 6. 异常处理与降级&#10;&#10;- 网络错误、API 错误、解析错误统一落入 `AppUpdateCheckResult.Failed`。&#10;- 失败不会影响主业务功能，仅更新 `lastUpdateCheckError` 与时间戳。&#10;- 若未找到 APK 资产，不展示可下载更新入口（避免无效按钮）。&#10;&#10;## 7. 测试与验证建议&#10;&#10;### 7.1 已有单元测试&#10;&#10;- `app/src/test/java/com/kurosu/sleepin/data/update/VersionNameComparatorTest.kt`&#10;- `app/src/test/java/com/kurosu/sleepin/data/repository/SettingsRepositoryImplTest.kt`&#10;&#10;### 7.2 手工验证清单&#10;&#10;1. 在可联网环境手动点击“立即检查”，确认状态刷新。&#10;2. 模拟有新版本场景，确认启动后弹窗出现且显示 release notes。&#10;3. 点击“稍后”，重进应用确认同版本不重复弹窗。&#10;4. 点击“下载更新”，确认系统下载列表中出现 APK 任务。&#10;5. 断网后检查，确认不会崩溃且会记录错误信息。&#10;&#10;## 8. 后续扩展建议&#10;&#10;- 增加“下载完成后一键安装”引导（监听下载完成广播）。&#10;- 细化 release notes 渲染（支持 Markdown 基础样式）。&#10;- 增加“忽略此版本”与“重新提醒”的显式设置项。&#10;- 为 GitHub API 增加更细粒度重试与限流提示。&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/docs/SleepIn-Docs/docs/dev-feature/README.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/docs/SleepIn-Docs/docs/dev-feature/README.md" />
              <option name="updatedContent" value="---&#10;title: 单功能实现专栏&#10;createTime: 2026/03/26 23:30:00&#10;permalink: /dev-feature/&#10;---&#10;&#10;# 单功能实现专栏&#10;&#10;这个专栏聚焦 **单一功能** 的实现复盘，目标是让新同学可以快速回答下面三个问题：&#10;&#10;1. 这个功能解决什么业务问题。&#10;2. 这个功能在 SleepIn 的分层架构里落在哪里。&#10;3. 这个功能上线后如何排查、验证和扩展。&#10;&#10;## 内容组织方式&#10;&#10;- 每篇文档只讲一个功能，不混杂多个主题。&#10;- 先给业务视角，再给代码入口，再给数据流和生命周期。&#10;- 最后提供测试清单与扩展建议，方便后续迭代。&#10;&#10;## 当前文章&#10;&#10;- [应用更新检测与下载实现（GitHub + WorkManager + DownloadManager）](./1.update-check.md)&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>