package com.kurosu.sleepin

import android.app.Application

/**
 * Application entry point.
 * Reserved for future global initialization (DI, logging, startup tasks).
 */
class SleepInApplication : Application()
